spring:
  config:
    activate:
      on-profile:
      - common-module
      
  mandatory-file-encoding: UTF-8        
                  
server:
  tomcat:
    connection-timeout: 2s
    keep-alive-timeout: 15s
    threads:
      max: 50
      min-spare: 5